# Sensitive config (lives outside web root on server)
adcontent_config.php
config.php
.env

# adcontent_config.php is used on cPanel only — secrets are env vars on Vercel

# PHP/Python artifacts
.venv/
__pycache__/
**/__pycache__/
*.pyc
